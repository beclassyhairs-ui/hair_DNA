// ============================================================================
// lib/consultChannel.ts — 문의 창구(카카오 채널) 공용 플래그·링크 단일 소스.
//   홈 + 결과지(스타일·데미지)가 같은 값을 참조한다. URL을 바꿀 땐 여기 한 곳만 고친다.
//   enabled && href 둘 다 있어야 노출 → "눌렀는데 갈 곳 없음"을 원천 차단.
//   ★ 채팅 URL(/chat)이라 탭하면 바로 채팅창이 열린다(채널 홈 URL 아님).
//   채널명: 미알팁 / 아이디: @mialtip
// ============================================================================

export const CONSULT_CHANNEL = {
  enabled: true,
  href: "http://pf.kakao.com/_gkXJX/chat",
} as const;
