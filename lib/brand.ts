// ============================================================================
// lib/brand.ts — 서비스 브랜드명 단일 소스 (미확정, 임시 "어뷰티")
//
// ⚠️ 브랜드/서비스명이 확정되면 이 파일의 값만 바꾼다(신규 코드는 전부 여기서 참조).
//    단, 아래 두 곳은 코드 밖이라 별도로 함께 바꿔야 한다:
//      1) 카카오 developers 콘솔 "앱 이름" — 카카오 로그인 동의창 상단에 뜨는 이름
//      2) lib/business.ts 의 상호(companyName) — 전자상거래법 사업자 표시(법인명, 별개일 수 있음)
//
// 📌 기존 ~50개 파일에 하드코딩된 "어뷰티"/"A-Beauty"는 이번에 치환하지 않는다(백로그).
//    신규 카카오 로그인 표면(버튼·모달·동의 카피)만 이 상수를 처음부터 사용한다.
// ============================================================================

export const BRAND = {
  /** 한글 서비스명(임시) */
  serviceName: "어뷰티",
  /** 로마자 표기 */
  serviceNameEn: "A-Beauty",
  /** 병기 표기(문서/타이틀용) */
  full: "어뷰티(A-Beauty)",
  /** 한 줄 소개 */
  tagline: "AI 헤어 진단 & 발견템",
} as const;

/** "{서비스명}에 로그인" — 카카오 로그인 버튼/모달 카피용 파생 문구 */
export const loginTitle = `${BRAND.serviceName} 로그인`;
