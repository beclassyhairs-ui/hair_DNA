// ============================================================================
// lib/brand.ts — 서비스 브랜드명 단일 소스 (확정: "미알팁")
//
// ⚠️ 브랜드/서비스명이 확정되면 이 파일의 값만 바꾼다(신규 코드는 전부 여기서 참조).
//    단, 아래 두 곳은 코드 밖이라 별도로 함께 바꿔야 한다:
//      1) 카카오 developers 콘솔 "앱 이름" — 카카오 로그인 동의창 상단에 뜨는 이름
//      2) lib/business.ts 의 상호(companyName) — 전자상거래법 사업자 표시(법인명, 별개일 수 있음)
//
// 📌 이 상수는 아직 어느 화면에도 import되지 않는다(연결 0건). 값만 최신 서비스명으로
//    맞춰 둔 상태다 — SSOT로 배선하는 리팩터링은 별건(회귀 위험). 화면 텍스트는 각 파일에
//    직접 "미알팁"으로 교체돼 있다(서비스명 전면 교체 라운드, 2026-08).
// ============================================================================

export const BRAND = {
  /** 한글 서비스명 */
  serviceName: "미알팁",
  /** 로마자 표기 */
  serviceNameEn: "MIALTIP",
  /** 병기 표기(문서/타이틀용) */
  full: "미알팁",
  /** 한 줄 소개(태그라인) */
  tagline: "미용실에서 알 수 없는 꿀팁",
} as const;

/** "{서비스명}에 로그인" — 카카오 로그인 버튼/모달 카피용 파생 문구 */
export const loginTitle = `${BRAND.serviceName} 로그인`;
