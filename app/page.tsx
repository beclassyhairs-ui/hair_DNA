import { redirect } from "next/navigation";

// ============================================================================
// 어뷰티(A-Beauty) — 루트 랜딩 (/)
//
// 별도의 사본 랜딩을 유지하지 않고, 진단 랜딩 4종과 동일한 화이트 톤·로직을
// 가진 /style을 유일한 원본으로 삼아 그대로 리다이렉트한다. 플래그십 모듈이
// 바뀌면 이 한 줄만 바꾸면 된다.
// ============================================================================

export default function RootPage() {
  redirect("/style");
}
