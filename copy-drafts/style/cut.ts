// style/cut — 커트·기장 카피. §7-1 저장 구조.
// 다음 단계에서 채울 슬롯: q11_length × q14_layer 기반 커트 판단.
//   현행 갈래 원고의 procedure 필드(시술 참고) 중 커트 관련 문장이 이관 대상.
import type { StyleCopyBlockModule } from "../types";

const cut: StyleCopyBlockModule = {
  domain: "style",
  block: "cut",
  entries: [],
};
export default cut;
